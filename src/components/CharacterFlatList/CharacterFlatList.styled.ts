import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    width: '100%',
  },
  contentContainer: {
    gap: 24,
    paddingVertical: 16,
    paddingHorizontal: 16,
  },
});
